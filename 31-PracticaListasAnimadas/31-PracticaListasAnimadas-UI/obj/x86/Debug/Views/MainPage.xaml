<Page
    x:Class="_31_PracticaListasAnimadas_UI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_31_PracticaListasAnimadas_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:_31_PracticaListasAnimadas_UI.ViewModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:MainPageVM></vm:MainPageVM>
    </Page.DataContext>
    <Page.Resources>
        <Storyboard x:ConnectionId='2' x:Name="storyBoardGradent" RepeatBehavior="Forever" AutoReverse="True">
            <ColorAnimation  Storyboard.TargetName="btnGradiel01"  
                            Storyboard.TargetProperty = "(Button.Background).(SolidColorBrush.Color)"  
                            From="Red" To="Orange" Duration="0:0:5"/>
        </Storyboard>
    </Page.Resources>

    <RelativePanel>
        <TextBlock x:ConnectionId='3' FontSize="30" Name="texto">EMPLEADOS</TextBlock>
        <ListView x:ConnectionId='4' Name="listView" RelativePanel.AlignLeftWithPanel="True" RelativePanel.Below="texto" ItemsSource="{Binding ListadoPersonas, Mode=TwoWay}" SelectedItem="{Binding PersonaSeleccionada, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Name="n" Text="{Binding Path=Nombre}"/>
                        <TextBlock Margin="3,0,0,0" Name="ap" Text="{Binding Path=Apellidos}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button x:ConnectionId='5' x:Name="btnGradiel01" RelativePanel.RightOf="listView" RelativePanel.AlignTopWithPanel="True" Content="Change my color!"                           >
            <!--<Button.Background>
                <LinearGradientBrush  >
                    <GradientStop Color="Red" Offset="0.0" />
                    <GradientStop Color="Orange" Offset="0.40" />
                    <GradientStop Color="Green" Offset="0.60" />
                    <GradientStop Color="Blue" Offset="0.80" />
                    <GradientStop Color="Black" Offset="1.00" />
                </LinearGradientBrush >
            </Button.Background>-->
        </Button>

      
    </RelativePanel>
</Page>

